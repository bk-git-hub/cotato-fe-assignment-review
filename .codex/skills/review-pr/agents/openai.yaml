interface:
  display_name: "Review PR"
  short_description: "Review a student assignment pull request from a PR number, URL, or the oldest open PR by default"
  default_prompt: "Use this skill to review a student assignment PR from a PR URL, PR number, or the oldest currently open PR when no identifier is provided. Compare against the student base branch, read the student review log, and produce Korean inline review comments with explanations."
